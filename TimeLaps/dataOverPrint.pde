/*PGraphics renderScrn;

PImage printClock() {
  renderScrn = createGraphics(width, height,P2D); 
  font = loadFont("data/gotham_72.vlw");
  textFont(font, 32);
  text("word", 10, 50);
  PImage renderedClock;
  return renderedClock;
} 

PImage printGraph() {
  PImage graph;
  return graph;
}

PImage printMetadata(){
  PImage iMetadata;
  return iMetadata;
}
*/
